// 完整版菜单比较多，将 rank 抽离出来，在此方便维护

const home = 0, // 平台规定只有 home 路由的 rank 才能为 0 ，所以后端在返回 rank 的时候需要从非 0 开始
  course = 1,
  resource = 2,
  disk = 3,
  analysis = 4,
  content = 5,
  result = 8,
  error = 9,
  frame = 10,
  permission = 12,
  system = 13,
  monitor = 14,
  curriculum = 15,
  codemirror = 17,
  able = 28,
  table = 30,
  markdown = 31,
  form = 31;
export {
  home,
  course,
  resource,
  analysis,
  disk,
  able,
  table,
  form,
  result,
  error,
  frame,
  permission,
  system,
  monitor,
  curriculum,
  codemirror,
  markdown,
  content,
};
